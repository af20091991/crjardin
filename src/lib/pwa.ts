// Manifest-only installability. No service worker = no offline caching.
// The browser offers "Add to Home Screen" solely from the manifest link in <head>.
export function registerPwa() {
  // no-op
}
